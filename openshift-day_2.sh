methods to create docker image: commit & Dockerfile
In Dockerfile: RUN (used when the command is supposed to run when an image is being created) Build time
			   CMD (used when the command is supposed to run when a container starts) Run time

Docker history registry:latest\

adding firewall exception permanently 
firewall-cmd --add-port=5000/tcp --permanent