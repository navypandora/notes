Network As a service
Switch or router(depends if they have the same netowrk or not) between 2 devices
Switch(Same network)
router(Different network)
Network Name: Dependent on the IP and netmask, if 192.168.0.1 is the IP, the netmask is 255.255.0.0 so network Name is 192.168
192,168.1.0/24 network name with netmask